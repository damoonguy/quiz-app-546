<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="public/css/index.css">
    <title>Document</title>
</head>
<body>
    
    <div id="greeting">
    {{#if (isdefined user)}}
        <h1>Welcome, {{user.username}}!</h1>
            <div class="dashboard-stats">
                <div class="stat-card">
                    <h3>Your Quizzes</h3>
                    <p class="stat-number">{{user.quizzesTaken}}</p>
                    <p class="stat-label">Quizzes Taken</p>
                </div>
                <div class="stat-card">
                    <h3>Average Score</h3>
                    <p class="stat-number">{{user.averageScore}}%</p>
                    <p class="stat-label">Across All Quizzes</p>
                </div>
                <div class="stat-card">
                    <h3>Ranking</h3>
                    <p class="stat-number">#{{user.ranking}}</p>
                    <p class="stat-label">Among All Users</p>
                </div>
                <div class="stat-card">
                    <h3>Quizzes Created</h3>
                    <p class="stat-number">{{user.quizzesCreated}}</p>
                    <p class="stat-label">Total Created</p>
                </div>
            </div>
    {{else}}
        <h1>Welcome, Guest! </h1>
        <h2>Login or Register to start taking/creating quizzes!</h2>
    {{/if}}
    </div>
    
</body>
</html>
